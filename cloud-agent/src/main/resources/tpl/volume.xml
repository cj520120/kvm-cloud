<volume>
    <name>{{name}}</name>
    {% if capacity > 0 %}
    <capacity unit="bytes">{{capacity}}</capacity>
    {% endif %}
    {% if allocation > 0 %}
    <allocation unit="bytes">{{allocation}}</allocation>
    {% endif %}
    <target>
        <format type="{{format}}"/>
        <permissions>
            <mode>0600</mode>
            <owner>0</owner>
            <group>0</group>
        </permissions>
    </target>
</volume>