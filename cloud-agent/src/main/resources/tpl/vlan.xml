<network>
    <name>{{name}}</name>
    <forward mode='bridge'/>
    <bridge name='{{bridge}}'/>
    <virtualport type='openvswitch'/>
    <portgroup name='vlan-{{vlanId}}' default='yes'>
        <vlan>
            <tag id="{{vlanId}}"/>
        </vlan>
    </portgroup>
</network>
